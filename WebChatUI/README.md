1. start docker
2. run `docker compose up -d` and open `localhost:3104` in a browser. To bypass the login page for a single-user setup, set the `WEBUI_AUTH` environment variable to False 
3. setup connection to your backend endpoint (local or remote)